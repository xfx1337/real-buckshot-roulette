# Тяжёлое и ненужное серверу в образе.
venv/
__pycache__/
*.pyc
.pytest_cache/
.git/
.vscode/
.DS_Store

# config.json монтируется томом на старте (см. docker-compose.yml), не в образ —
# так правки сети/IP применяются без пересборки. Пароль Wi-Fi тоже не запекаем.
config.json
esp/config.h

# Прошивка ESP32 и всё вокруг неё — хостовая часть, в серверный образ не нужна.
esp/
scripts/

# Godot-исходники (большие бинарные ассеты, к веб-серверу отношения не имеют).
Buckshot Roulette/
Buckshot Roulette веб-дизайн/

# Документация/прочее.
*.md
