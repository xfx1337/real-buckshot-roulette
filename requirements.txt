fastapi==0.115.12
uvicorn[standard]==0.34.3
websockets==15.0.1
jinja2==3.1.6
python-multipart==0.0.20

# Серверный звук (app/audio_engine.py): проигрывание озвучки из Python на
# выбранное устройство вывода, минуя браузер. Необязательны — без них сервер
# работает, а панель дилера остаётся на браузерном звуке.
sounddevice==0.5.5
soundfile==0.14.0

# Веб-панель телефонии (voip/scripts/web.py): состояние трубок и звонок
# кнопкой. Отдельный от игрового сервера процесс на :8080, поэтому и фреймворк
# у него свой — переписывать его на FastAPI ради одной зависимости незачем.
flask==3.1.3
