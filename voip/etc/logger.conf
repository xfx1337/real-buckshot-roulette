[general]
dateformat = %F %T

[logfiles]
; Everything, including the dialplan trace. "why did that call do nothing" is
; unanswerable after the fact without the Executing lines naming each step and
; the sound file it opened.
full => notice,warning,error,verbose,dtmf
; Errors only, for reading at a glance.
messages => notice,warning,error
