[general]
; A narrow range, so what the gateway sends can be told apart from unrelated
; traffic when watching with tcpdump.
rtpstart=10000
rtpend=10200
; Both ends are on the same wire; ICE has nothing to discover and only delays
; the first packet.
icesupport=no
stunaddr=
